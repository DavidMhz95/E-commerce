{
  "name": "black.market.node",
  "version": "1.0.0",
  "description": "Sistema de búsqueda y recuperación de contenidos almacenados en el sistema 'Black Market' propiedad del trio de oro (MAD)",
  "main": "dist/app.js",
  "scripts": {
    "buildAndRun": "(if exist dist rmdir dist /q /s) && tsc && copyfiles servicesettings.json dist && copyfiles --flat src/images/*.* dist  && copyfiles --flat src/views/*.* dist && copyfiles package.json dist && node dist/app.js",
    "build": "(if exist dist rmdir dist /q /s) && tsc && copyfiles servicesettings.json dist && copyfiles --flat src/images/*.* dist  && copyfiles --flat src/views/*.* dist && copyfiles package.json dist",
    "start": "node dist/app.js",
    "test": "mocha"
  },
  "author": "MAD",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.7",
    "copyfiles": "^2.3.0",
    "mocha": "^8.1.3",
    "request": "^2.88.2",
    "tslint": "^6.1.2",
    "typescript": "^3.9.6"
  },
  "dependencies": {
    "@elastic/elasticsearch": "^7.8.0",
    "black-market-model": "file:../model",
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "compression": "^1.7.4",
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "elastic-builder": "^2.7.0",
    "express": "^4.17.1",
    "http-errors": "^1.8.0",
    "morgan": "^1.10.0",
    "rxjs": "^6.6.0",
    "serve-favicon": "^2.5.0",
    "ts-md5": "^1.2.7",
    "validator": "^13.1.1"
  }
}
